# Next.js
.next/
out/
next-env.d.ts

# Claude Code internal
.claude/

# Env files — NEVER commit real keys
.env.local
.env.*.local

# Dependencies
node_modules/

# Misc
.DS_Store
*.pem

# Firebase service account keys — never commit
*-firebase-adminsdk-*.json
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vercel
tsconfig.tsbuildinfo

# Live-rules dumps written by scripts/compare-rules.js when it finds drift
live-*.rules.tmp

# BATS CRM exports — real client/carrier data, dropped in the root for the
# importer. Never commit them.
*-export-*.csv
